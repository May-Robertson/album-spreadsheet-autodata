Please run from the terminal with command "python3 albums-metadata.py"

install packages in requirements.txt

include google sheets spreadsheet id in a file called .env in the same directory as albums-metadata.py 